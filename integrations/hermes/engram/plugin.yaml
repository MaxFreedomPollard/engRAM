name: engram
version: 1.7.0
description: "engRAM - high-security, fully offline, encrypted vector memory. AEAD-encrypted vault, locked-by-default after restart, native Hermes provider, no API key."
pip_dependencies: []
requires_env: []
hooks:
  - on_session_switch

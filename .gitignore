# NEVER commit the first-party pack-signing private key
tools/pack_identity.json

# local environment / build artifacts
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.DS_Store

# local vaults and scratch (vaults are user data, not source)
*.vault
*.vault.config.json
*.vault.flock
engram-airgap.zip

# AKC raw source datasets (build-time only; the built .mpack is what ships)
tools/akc_source/
